O Feature Set.
          </h1>

          <p className="mx-auto mt-6 max-w-3xl text-lg text-muted-foreground md:text-xl">
            Everything you need to research keywords, audit sites, track rankings, and generate SEO content
            with a single modern platform.
          </p>

          <div className="mt-8 flex flex-col items-center justify-center gap-4 sm:flex-row">
            <Button size="lg" variant="premium" asChild>
              <Link to="/signup">
                Start Free Trial
                <ArrowRight className="ml-2 h-4 w-4" />
              </Link>
            </Button>
            <Button size="lg" variant="outline" asChild>
              <Link to="/pricing">View Pricing</Link>
            </Button>
          </div>

          <div className="mt-10 flex flex-wrap items-center justify-center gap-3 text-sm text-muted-foreground">
            {highlights.map((item) => (
              <span key={item} className="inline-flex items-center gap-2 rounded-full border bg-background px-4 py-2 shadow-sm">
                <CheckCircle2 className="h-4 w-4 text-primary" />
                {item}
              </span>
            ))}
          </div>
        </div>
      </section>

      {/* Feature grid */}
      <section className="py-24">
        <div className="container mx-auto max-w-7xl px-6 lg:px-12">
          <div className="mx-auto mb-16 max-w-3xl text-center">
            <h2 className="text-balance text-3xl font-bold tracking-tight md:text-5xl">
              Built for every part of your SEO workflow.
            </h2>
            <p className="mt-4 text-lg text-muted-foreground">
              From discovery to execution, Hi-SEO keeps your team moving with one connected workspace.
            </p>
          </div>

          <div className="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
            {features.map((feature) => {
              const Icon = feature.icon
              return (
                <Card
                  key={feature.title}
                  className="group border-border/60 bg-background p-6 shadow-soft transition-all duration-300 hover:-translate-y-1 hover:border-primary/50"
                >
                  <div className="flex h-12 w-12 items-center justify-center rounded-xl bg-primary/10 text-primary transition-all duration-300 group-hover:scale-110 group-hover:bg-primary group-hover:text-white">
                    <Icon className="h-6 w-6" />
                  </div>
                  <h3 className="mt-5 text-xl font-bold tracking-tight">{feature.title}</h3>
                  <p className="mt-3 leading-relaxed text-muted-foreground">{feature.description}</p>
                </Card>
              )
            })}
          </div>
        </div>
      </section>

      {/* Workflow */}
      <section className="border-y bg-card py-24">
        <div className="container mx-auto max-w-7xl px-6 lg:px-12">
          <div className="grid gap-12 lg:grid-cols-2 lg:items-center">
            <div>
              <span className="inline-flex items-center gap-2 rounded-full border border-primary/15 bg-primary/10 px-4 py-1.5 text-xs font-semibold uppercase tracking-[0.2em] text-primary">
                How it works
              </span>
              <h2 className="mt-6 text-balance text-3xl font-bold tracking-tight md:text-5xl">
                A simple workflow for serious growth teams.
              </h2>
              <p className="mt-4 max-w-xl text-lg text-muted-foreground">
                Use Hi-SEO to understand what matters, fix what is broken, and measure what is working.
              </p>

              <div className="mt-8 space-y-4">
                {steps.map((item) => (
                  <div key={item.step} className="flex gap-4 rounded-2xl border bg-background p-5 shadow-soft">
                    <div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-primary text-sm font-bold text-primary-foreground">
                      {item.step}
                    </div>
                    <div>
                      <h3 className="text-lg font-semibold">{item.title}</h3>
                      <p className="mt-1 text-sm text-muted-foreground">{item.text}</p>
                    </div>
                  </div>
                ))}
              </div>
            </div>

            <div className="rounded-3xl border bg-gradient-to-br from-primary/10 via-background to-indigo-500/10 p-8 shadow-soft">
              <div className="rounded-2xl border bg-background p-6 shadow-soft">
                <p className="text-sm font-semibold text-muted-foreground">Live dashboard snapshot</p>
                <div className="mt-4 grid grid-cols-2 gap-4">
                  <Card className="p-4">
                    <p className="text-xs text-muted-foreground">SEO Score</p>
                    <p className="mt-2 text-3xl font-bold text-primary">92</p>
                  </Card>
                  <Card className="p-4">
                    <p className="text-xs text-muted-foreground">Traffic Growth</p>
                    <p className="mt-2 text-3xl font-bold text-emerald-600">+18%</p>
                  </Card>
                  <Card className="p-4">
                    <p className="text-xs text-muted-foreground">Keywords Tracked</p>
                    <p className="mt-2 text-3xl font-bold">24.6K</p>
                  </Card>
                  <Card className="p-4">
                    <p className="text-xs text-muted-foreground">Backlinks</p>
                    <p className="mt-2 text-3xl font-bold">3.4K</p>
                  </Card>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* CTA */}
      <section className="py-24">
        <div className="container mx-auto max-w-4xl px-6 lg:px-12 text-center">
          <h2 className="text-balance text-4xl font-bold tracking-tight md:text-5xl">
            Ready to see the full platform in action?
          </h2>
          <p className="mx-auto mt-6 max-w-2xl text-lg text-muted-foreground">
            Start your free trial or compare plans to see which Hi-SEO package fits your team best.
          </p>

          <div className="mt-10 flex flex-col items-center justify-center gap-4 sm:flex-row">
            <Button size="lg" variant="premium" className="w-full px-10 sm:w-auto" asChild>
              <Link to="/signup">Start Free Trial</Link>
            </Button>
            <Button size="lg" variant="outline" className="w-full px-10 sm:w-auto" asChild>
              <Link to="/pricing">Compare Plans</Link>
            </Button>
          </div>
        </div>
      </section>
    </div>
  )
}


--- FILE: src\pages\public\ForgotPassword.tsx ---

import { FormEvent, useState } from "react"
import { Link } from "react-router-dom"
import { ArrowRight, Mail, ShieldCheck, Sparkles } from "lucide-react"
import { supabase } from "../../lib/supabase"
import { Button } from "../../components/ui/button"
import { Card } from "../../components/ui/card"

export default function ForgotPassword() {
  const [email, setEmail] = useState("")
  const [loading, setLoading] = useState(false)
  const [message, setMessage] = useState("")
  const [error, setError] = useState("")

  const handleResetRequest = async (e: FormEvent<HTMLFormElement>) => {
    e.preventDefault()
    setLoading(true)
    setMessage("")
    setError("")

    try {
      const { error } = await supabase.auth.resetPasswordForEmail(email, {
        redirectTo: `${window.location.origin}/reset-password`,
      })

      if (error) {
        setError(error.message)
        return
      }

      setMessage("Password reset email sent. Check your inbox.")
      setEmail("")
    } catch {
      setError("Something went wrong. Please try again.")
    } finally {
      setLoading(false)
    }
  }

  return (
    <div className="min-h-screen bg-gradient-to-b from-background to-muted/30 px-4 py-10">
      <div className="mx-auto grid min-h-[calc(100vh-5rem)] max-w-7xl items-center gap-10 lg:grid-cols-2">
        {/* Left side branding */}
        <div className="hidden lg:block space-y-8">
          <span className="inline-flex items-center gap-2 rounded-full border border-primary/15 bg-primary/10 px-4 py-1.5 text-xs font-semibold uppercase tracking-[0.2em] text-primary">
            <Sparkles className="h-4 w-4" />
            Password recovery
          </span>

          <div className="space-y-5">
            <h1 className="text-balance text-5xl font-bold tracking-tight xl:text-6xl">
              Reset your Hi-SEO password safely.
            </h1>
            <p className="max-w-xl text-lg text-muted-foreground">
              We’ll send a secure reset link so you can get back into your SEO workspace quickly.
            </p>
          </div>

          <div className="grid max-w-xl gap-4 sm:grid-cols-2">
            {[
              "Secure email reset",
              "Quick account recovery",
              "Works with Supabase auth",
              "Back to work in minutes",
            ].map((item) => (
              <div key={item} className="flex items-center gap-3 rounded-2xl border bg-background/80 p-4 shadow-soft">
                <ShieldCheck className="h-5 w-5 text-primary" />
                <span className="text-sm font-medium">{item}</span>
              </div>
            ))}
          </div>
        </div>

        {/* Reset form */}
        <Card className="mx-auto w-full max-w-md border-border/60 bg-background p-8 shadow-premium">
          <div className="mb-6">
            <h2 className="text-3xl font-bold tracking-tight">Forgot password</h2>
            <p className="mt-2 text-sm text-muted-foreground">
              Enter your email and we’ll send you a reset link.
            </p>
          </div>

          <form onSubmit={handleResetRequest} className="space-y-4">
            <div className="space-y-2">
              <label htmlFor="email" className="text-sm font-medium">
                Email
              </label>
              <div className="relative">
                <Mail className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" />
                <input
                  id="email"
                  type="email"
                  placeholder="you@example.com"
                  className="w-full rounded-xl border border-input bg-background px-4 py-3 pl-10 outline-none transition focus:border-primary"
                  value={email}
                  onChange={(e) => setEmail(e.target.value)}
                  required
                />
              </div>
            </div>

            {error && (
              <div className="rounded-xl border border-destructive/20 bg-destructive/10 px-4 py-3 text-sm text-destructive">
                {error}
              </div>
            )}

            {message && (
              <div className="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">
                {message}
              </div>
            )}

            <Button type="submit" className="w-full" variant="premium" disabled={loading}>
              {loading ? "Sending..." : "Send reset link"}
              {!loading && <ArrowRight className="ml-2 h-4 w-4" />}
            </Button>
          </form>

          <p className="mt-6 text-center text-sm text-muted-foreground">
            Remember your password?{" "}
            <Link to="/login" className="font-medium text-primary hover:underline">
              Log in
            </Link>
          </p>
        </Card>
      </div>
    </div>
  )
}


--- FILE: src\pages\public\Glossary.tsx ---

import { Link } from "react-router-dom"
import { Button } from "../../components/ui/button"
import { Card } from "../../components/ui/card"
import { BookOpen, ArrowRight, Sparkles, CheckCircle2 } from "lucide-react"

const categories = [
  {
    title: "Technical SEO",
    items: [
      { term: "Crawl Budget", definition: "The number of pages search engines crawl on your site within a given period." },
      { term: "Canonical Tag", definition: "A signal that tells search engines which version of a page should be indexed." },
      { term: "Core Web Vitals", definition: "Google’s performance metrics for loading, interactivity, and visual stability." },
    ],
  },
  {
    title: "Content SEO",
    items: [
      { term: "Search Intent", definition: "The reason behind a user’s search query and what result they want." },
      { term: "Topic Cluster", definition: "A group of related content built around one main subject and linked together." },
      { term: "Keyword Cannibalization", definition: "When multiple pages compete for the same keyword and weaken rankings." },
    ],
  },
  {
    title: "Link Building",
    items: [
      { term: "Backlink", definition: "A link from one website to another, often used as a ranking signal." },
      { term: "Domain Authority", definition: "A popularity/strength metric used to estimate a site’s ranking ability." },
      { term: "Anchor Text", definition: "The clickable text used in a hyperlink." },
    ],
  },
  {
    title: "Analytics & Reporting",
    items: [
      { term: "CTR", definition: "Click-through rate: the percentage of impressions that become clicks." },
      { term: "Organic Traffic", definition: "Visitors who land on your site from unpaid search results." },
      { term: "Impressions", definition: "How many times your pages appear in search results." },
    ],
  },
]

const highlights = [
  "400+ SEO terms",
  "Easy-to-browse categories",
  "Beginner-friendly definitions",
  "Built for marketers and teams",
]

export default function Glossary() {
  return (
    <div className="flex min-h-screen flex-col">
      {/* Hero */}
      <section className="border-b bg-muted/10 py-24">
        <div className="container mx-auto max-w-7xl px-6 lg:px-12 text-center">
          <span className="inline-flex items-center gap-2 rounded-full border border-primary/15 bg-primary/10 px-4 py-1.5 text-xs font-semibold uppercase tracking-[0.2em] text-primary">
            <BookOpen className="h-4 w-4" />
            SEO knowledge base
          </span>

          <h1 className="mt-6 text-balance text-4xl font-bold tracking-tight md:text-6xl">
            SEO Glossary
          </h1>

          <p className="mx-auto mt-6 max-w-3xl text-lg text-muted-foreground md:text-xl">
            Your comprehensive guide to SEO terms, concepts, and best practices — explained simply for modern teams.
          </p>

          <div className="mt-8 flex flex-col items-center justify-center gap-4 sm:flex-row">
            <Button size="lg" variant="premium" asChild>
              <Link to="/signup">
                Start Free Trial
                <ArrowRight className="ml-2 h-4 w-4" />
              </Link>
            </Button>
            <Button size="lg" variant="outline" asChild>
              <Link to="/features">Explore Features</Link>
            </Button>
          </div>

          <div className="mt-8 flex flex-wrap items-center justify-center gap-3 text-sm text-muted-foreground">
            {highlights.map((item) => (
              <span key={item} className="inline-flex items-center gap-2 rounded-full border bg-background px-4 py-2 shadow-sm">
                <CheckCircle2 className="h-4 w-4 text-primary" />
                {item}
              </span>
            ))}
          </div>
        </div>
      </section>

      {/* Categories */}
      <section className="py-24">
        <div className="container mx-auto max-w-7xl px-6 lg:px-12">
          <div className="mx-auto mb-16 max-w-3xl text-center">
            <h2 className="text-balance text-3xl font-bold tracking-tight md:text-5xl">
              Browse glossary terms by category.
            </h2>
            <p className="mt-4 text-lg text-muted-foreground">
              Start with the most common SEO concepts your team uses every day.
            </p>
          </div>

          <div className="grid gap-8 md:grid-cols-2">
            {categories.map((category) => (
              <Card key={category.title} className="border-border/60 bg-background p-6 shadow-soft">
                <div className="flex items-center gap-3">
                  <div className="flex h-12 w-12 items-center justify-center rounded-xl bg-primary/10 text-primary">
                    <Sparkles className="h-6 w-6" />
                  </div>
                  <div>
                    <h3 className="text-xl font-bold tracking-tight">{category.title}</h3>
                    <p className="text-sm text-muted-foreground">{category.items.length} key terms</p>
                  </div>
                </div>

                <div className="mt-6 space-y-4">
                  {category.items.map((item) => (
                    <div key={item.term} className="rounded-2xl border bg-muted/20 p-4">
                      <p className="font-semibold">{item.term}</p>
                      <p className="mt-1 text-sm leading-relaxed text-muted-foreground">{item.definition}</p>
                    </div>
                  ))}
                </div>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* Intro section */}
      <section className="border-y bg-card py-24">
        <div className="container mx-auto max-w-5xl px-6 lg:px-12 text-center">
          <h2 className="text-balance text-3xl font-bold tracking-tight md:text-5xl">
            Learn the language of modern SEO.
          </h2>
          <p className="mx-auto mt-6 max-w-3xl text-lg text-muted-foreground">
            Whether you’re building content, running audits, or reporting to clients, understanding SEO terminology helps your team move faster and make better decisions.
          </p>
        </div>
      </section>

      {/* CTA */}
      <section className="py-24">
        <div className="container mx-auto max-w-4xl px-6 lg:px-12 text-center">
          <h2 className="text-balance text-4xl font-bold tracking-tight md:text-5xl">
            Ready to learn SEO faster?
          </h2>
          <p className="mx-auto mt-6 max-w-2xl text-lg text-muted-foreground">
            Use Hi-SEO to research, track, and explain SEO concepts in one connected workspace.
          </p>

          <div className="mt-10 flex flex-col items-center justify-center gap-4 sm:flex-row">
            <Button size="lg" variant="premium" className="w-full px-10 sm:w-auto" asChild>
              <Link to="/signup">Start Free Trial</Link>
            </Button>
            <Button size="lg" variant="outline" className="w-full px-10 sm:w-auto" asChild>
              <Link to="/pricing">View Pricing</Link>
            </Button>
          </div>
        </div>
      </section>
    </div>
  )
}


--- FILE: src\pages\public\Home.tsx ---

import { HeroSection } from "../../components/marketing/HeroSection"
import { TrustedBySection } from "../../components/marketing/TrustedBySection"
import { FeaturesSection } from "../../components/marketing/FeaturesSection"
import { MetricsSection } from "../../components/marketing/MetricsSection"
import { CTASection } from "../../components/marketing/CTASection"

export default function Home() {
  return (
    <div className="flex min-h-screen flex-col overflow-hidden">
      <HeroSection />
      <TrustedBySection />
      <FeaturesSection />
      <MetricsSection />
      <CTASection />
    </div>
  )
}


--- FILE: src\pages\public\Login.tsx ---

import { FormEvent, useState } from "react"
import { Link, useNavigate } from "react-router-dom"
import { Eye, EyeOff, ArrowRight, ShieldCheck, Sparkles } from "lucide-react"
import { useAuth } from "../../context/AuthContext"
import { Button } from "../../components/ui/button"
import { Card } from "../../components/ui/card"

export default function Login() {
  const { signIn } = useAuth()
  const navigate = useNa
